import { splitProps } from "solid-js";
import { Route, Router } from "@solidjs/router";

export function PartasImportAttr(props) {
    const [PARTAS_LOCAL, PARTAS_OTHERS] = splitProps(props, []);
    return <Router>
        <Route />
    </Router>;
}

