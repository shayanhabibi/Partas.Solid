
import { omit } from "solid-js";
import { defaultOf } from "../../fable_modules/fable-library-js.5.13.0/Util.js";

export function TestStringAccess(props) {
    const PARTAS_OTHERS = omit(props, "index");
    const context = defaultOf();
    return <div>
        {context.value()[props.index]}
    </div>;
}
