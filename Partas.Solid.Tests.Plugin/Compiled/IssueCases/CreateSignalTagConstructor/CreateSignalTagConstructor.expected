import { Match, createSignal } from "solid-js";
import { defaultOf } from "../../fable_modules/fable-library-js.5.0.0-alpha.13/Util.js";
import { fakeTag } from "fakeLibrary";
import { FakeImport } from "FakeLibrary";

export function App_App() {
    const patternInput = createSignal([{
        title: "Home",
        url: "#",
        icon: <div />,
    }]);
    const patternInput_1 = createSignal(defaultOf());
    return <div>
        <fakeTag items={patternInput[0]()} />
        <FakeImport />
        <Match />
    </div>;
}

